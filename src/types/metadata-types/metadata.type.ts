export type MetadataTypes = {
  title: string;
  description: string;
  url: string;
  image: string;
};

export interface Metadata {
  title?: string;
  description?: string;
  url?: string;
  image?: string;
}
